// import './bootstrap';

// import Alpine from 'alpinejs';

// window.Alpine = Alpine;

// Alpine.start();

$('#content').trumbowyg({
    btns: [['formatting'],['strong', 'em',],['unorderedList', 'orderedList'],
        ['justifyLeft', 'justifyCenter', 'justifyRight', 'justifyFull']],
    autogrow: true
});
